// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // deep purple
    },
    secondary: {
      main: '#232023',
    },
  },
});

export default theme;
